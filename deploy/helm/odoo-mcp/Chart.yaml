apiVersion: v2
name: odoo-mcp
description: Odoo MCP Server — a Go service that exposes Odoo ERP models through the Model Context Protocol (MCP)
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: valkey
    version: ">=2.0.0"
    repository: https://charts.bitnami.com/bitnami
    condition: valkey.enabled